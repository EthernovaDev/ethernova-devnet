{
  "name": "ethernova-phase5-brutal-test",
  "version": "1.0.0",
  "description": "Phase 5 State Lifecycle Tiers — comprehensive brutal test suite (real network, multi-node consensus)",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "node scripts/run-all.js",
    "report": "node scripts/print-report.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0"
  }
}
